<?xml version="1.0"?>
<package format="3">
  <name>loc_fusion</name>
  <version>0.1.0</version>

  <description>
    ZED 2i + IMU + GNSS sensor-fusion stack.  It produces:
      • odom → base_link TF  (smooth short-term odom)
      • map → odom   TF  (GPS-corrected global pose)
      • /odometry/filtered  (odom frame)
      • /odometry/global    (map frame)
  </description>

  <maintainer email="dorde_ivanovic@college.harvard.edu">Dorde Ivanovic</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <exec_depend>robot_localization</exec_depend>
  <exec_depend>launch</exec_depend>
  <exec_depend>launch_ros</exec_depend>
</package>
