# =============================================================================
# GO TO NAMED POSE ACTION (Simplified for URC)
# =============================================================================
# Move arm to a pre-defined named pose from SRDF
# =============================================================================

# GOAL
string pose_name                    # Named pose from SRDF (e.g., "home", "es_ready")
float32 velocity_scaling 0.1        # Velocity scaling factor [0.0-1.0]

---

# RESULT  
bool success                        # True if pose reached successfully
string message                      # Error or success message

---

# FEEDBACK
string status                       # Current status message
